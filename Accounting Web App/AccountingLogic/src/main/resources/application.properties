# ===============================
# Database Configuration
# ===============================
spring.datasource.url=jdbc:mysql://accountingdatabase.ctsiaoa8yjzm.us-east-2.rds.amazonaws.com:3306/AccountingDB?useSSL=false&serverTimezone=UTC
spring.datasource.username=accounting
spring.datasource.password=accounting
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# Hibernate (JPA) Configuration
# ===============================

# JPA dialect for MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# DDL Mode: Update database schema automatically
spring.jpa.hibernate.ddl-auto=update

# Show SQL Queries (optional for debugging)
spring.jpa.show-sql=true

